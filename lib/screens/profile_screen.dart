import 'package:flutter/material.dart';

class ProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Profil'), backgroundColor: Colors.white, elevation: 0),
      body: Center(child: Text('Page de profil en construction')),
    );
  }
}